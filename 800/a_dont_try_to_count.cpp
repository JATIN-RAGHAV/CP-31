#include <iostream>
#include <string>
using namespace std;

void solver(){
        int n,m,subpos;
        cin >> n >> m;
        string x,s;
        cin >> x >> s;
        subpos = s.find(x);
        if(subpos != string::npos){
                
        }
}

int main(){
       int t;
       cin >> t;
       while(t--){
               solver();
       }
}
